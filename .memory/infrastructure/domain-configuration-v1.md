# Memory: infrastructure/domain-configuration-v1
Updated: just now

The platform is hosted on the custom subdomain 'app.eventpix.com.au'. DNS is configured with an A record pointing to '185.158.133.1' and a TXT record for verification. SSL is automatically provisioned. Existing 'eventpix-crm.lovable.app' links remain functional as both domains point to the same application instance. A public onboarding guide for crew is available at '/onboarding' without authentication, allowing it to be shared alongside invitations.
